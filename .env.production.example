# Copie para .env.production e preencha localmente. Não versione valores reais.
POSTGRES_DB=devflow
POSTGRES_USER=devflow
POSTGRES_PASSWORD=
JWT_SECRET=
CORS_ORIGIN=https://app.example.com
# Somente na primeira inicialização de um banco vazio:
APP_BOOTSTRAP_ENABLED=false
APP_BOOTSTRAP_NAME=
APP_BOOTSTRAP_EMAIL=
APP_BOOTSTRAP_PASSWORD=
